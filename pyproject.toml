[project]
name = "bi-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27,<1",
    "psycopg[binary]>=3.2,<4",
    "pydantic>=2,<3",
    "streamlit[auth]>=1.42,<2",
    "tzdata>=2024.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
